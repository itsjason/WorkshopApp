<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/main_wrapper"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:layout_marginTop="1dp"
    android:orientation="vertical"
    tools:context="com.jason.workshopapp.app.MainActivity">

    <TextView
        android:id="@+id/hello_text_view"
        android:text="@string/hello_world"
        android:layout_width="match_parent"
        android:layout_height="wrap_content" />

    <TextView
        android:textSize="40sp"
        android:textStyle="bold|italic"
        android:textColor="#995522"
        android:id="@+id/awesome_text"
        android:text="We are doing an Android Workshop!"
        android:layout_width="match_parent"
        android:layout_height="150dp"
        android:layout_below="@id/hello_text_view"
        android:gravity="center_horizontal|center_vertical"/>

    <Button
        android:id="@+id/awesome_button"
        android:layout_width="match_parent"
        android:layout_height="100dp"
        android:layout_below="@id/awesome_text"
        android:text="Hey You Should Click me!"
        android:background="#772222"/>

</LinearLayout>
